<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reja:</title>
    <link 
        rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        crossorigin="anonymous"
        />
</head>
<body style="background-color: hsl(233, 21%, 77%)">
    <div class="container">
        <h1 class="display-4 text-center py-1" style="font-family:Arial, Helvetica, sans-serif; margin-top:10px">REJA VA MAQSADLAR</h1>
        <div class="jumbotron p-3" style="border-radius:10px;background-color: hsl(0, 0%, 100%);">
            <form id="create-form" action="/create-item" method="POST">
                <div class="d-flex align-items-center">
                    <input
                        id="create-field"
                        name="reja" 
                        autofocus 
                        autocomplete="off"
                        class="form-control mr-3"
                        type="text"
                        style="flex: 1; border: 2px solid rgb(13, 110, 253)"
                    />
                    <button class="btn btn-primary" style="border-radius: 20px; margin-left: 13px">
                        REJA QO'SHISH
                    </button>
                </div>
            </form> 
        </div>
            <ul class="list-group mt-3 list-group-item-success">
                <% items.map(function(item) {%>
                <li class="list-group-item d-flex justify-content-between align-items-center" style="background-color: rgb(215, 227, 249);">
                    <%=item.reja %>
                    <div>
                        <button class="btn btn-outline-primary" data-id="<%= item._id %>">Edit</button>
                        <button class="btn btn-outline-danger" data-id="<%= item._id %>">Delete</button>
                    </div>
                </li>
                <% }); %>             
            </ul>

        <button id="clean-all" class="btn btn-danger" style="border: radius 20px; margin-top: 15px;">Hamma rejalarni o'chirish</button>
    </div>
</body>
</html>